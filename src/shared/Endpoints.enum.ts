export enum EndpointsList {
  PROJECTS = 'projects',
  SKILLS = 'skills',
  SOCIALS = 'socials',
}

export enum Endpoints {
  INFO = 'info',
  BIO = 'bio',
  MAIN_IMAGE = 'mainImage',
  AVATAR_IMAGE = 'avatarImage',
}
