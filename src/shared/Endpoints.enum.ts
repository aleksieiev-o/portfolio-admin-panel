export enum EndpointsList {
  PROJECTS = '/projects',
  SKILLS = '/skills',
  SOCIALS = '/socials',
  DOCUMENTS = '/documents',
}

export enum Endpoints {
  PERSONAL_INFO = '/personal-info',
  MAIN_IMAGE = '/main-image',
}
